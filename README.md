# Gear

Every pedal, amp, cable, and piece of gear I own — documented and mapped out.

Open `index.html` to view the interactive board. `calibrate.html` is a helper page for calibrating jack/pedal image coordinates against `config.dot`.
